HTML是一種網頁使用的語言，
是一種描述超文件的註記語言SGML(Standard Generalized Markup Language)所制訂出的一種網頁語言，
基本上現行的瀏覽器都可以讀取HTML，使用HTML可以編輯設計出網頁，
也可以在網頁中加入所有HTML語言可支援的方式，例如表格、表單、圖片、文字、連結、程式等等。


DOCTYPE是document type(文件型別)的簡寫,
在web設計中用來說明你用的XHTML或者HTML是什麼版本。

HTML5 是HTML最新的修訂版本，
廣義論及HTML5時，實際指的是包括HTML、CSS和JavaScript在內的一套技術組合



Markup標記是重點/也可稱為標籤

(段落開始)(paragraph 段落)  
    但是這樣寫本身是不符合規則的，因為網頁有主要她讀得懂的標記，
    這邊我們用英文paragraph，但是在網頁設計的規則他是使用paragraph的開頭p
(段落結束)
()這個括號在內文當中經常會用到，所以我們會把它換成<>角括號， /代表結束


因為我們正常人也都看得懂哪邊是文章哪邊是標題，那為什麼要用標籤呢?
因為今天不是每個人的想法都一樣，標籤也不是只給使用者來看，還有機器人
甚麼事機器人?搜尋引擎在看我們的網頁時是透過程式來去了解我們的網頁，
但是搜尋引擎沒有眼睛，所以她如何去閱讀我們的網頁?他認的就是標籤。
所以正確的標籤非常重要

w3school mdn


先從網頁來看裡面有哪些物件呢?
有 標題,內文,表格,清單,圖片,文章,超連結,粗體,斜體，還有包含聲音圖片
有順序的清單 ol
沒順序的清單 ul
所以大家對網頁有個基本的概念之後，我們就可以來mdn找有哪些標籤可以使用